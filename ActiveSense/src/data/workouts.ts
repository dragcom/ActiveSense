import { Workout } from '../types';

// This file is reserved for static workout data if the database facade is removed later.
export const workoutCategories: string[] = [];
// Workouts currently come from services/database.ts, so this stays empty for now.
export const workouts: Workout[] = [];
